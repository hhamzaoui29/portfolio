<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Liste des reservations</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/bootstrap.min.css">
  <link rel="stylesheet" href="/css/portfolio.css"> 
</head>
<body class="bg-light">
     <!-- Barre de navigation -->
     <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
      <div class="container">
        <!-- Bouton hamburger (mobile) -->
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
          <span class="navbar-toggler-icon"></span>
        </button>
        
        <div class="collapse navbar-collapse" id="navbarNav">
          <!-- Lien absolu vers ton portfolio -->
          <h1 class="h1">
            <a href="/projets" class="btn btn-secondary">⬅️ Retour à Liste Des Projets</a>
          </h1>
        </div>
      </div>
    </nav>
    
  <div class="container py-4">
    <h1 class="mb-4">📅 Liste des réservations</h1>

    <!-- bouton pour créer une réservation -->
    <a href="/reservations/formCreateReservation" class="btn btn-primary mb-3">➕ Nouvelle réservation</a>

    <!-- tableau des réservations -->
    <table class="table table-striped table-bordered">
      <thead class="table-dark">
        <tr>
          <th>ID</th>
          <th>Nom</th>
          <th>Email</th>
          <th>Téléphone</th>
          <th>Date</th>
          <th>Heure</th>
          <th>Service</th>
          <th>Actions</th>
          <th>Statut</th>
        </tr>
      </thead>
      <tbody>
        <% if (reservations.length > 0) { %>
          <% reservations.forEach(reservation => { %>
            <tr>
              <td><%= reservation.id %></td>
              <td><%= reservation.nom %></td>
              <td><%= reservation.email %></td>
              <td><%= reservation.telephone %></td>
              <td><%= reservation.date %></td>
              <td><%= reservation.heure %></td>
              <td><%= reservation.service %></td>
              <td><%= reservation.statut %></td>
              <td>
                <a href="/reservations/showReservation/<%= reservation.id %>" class="btn btn-sm btn-info">👁️ Voir</a>
            </tr>
          <% }) %>
        <% } else { %>
          <tr>
            <td colspan="8" class="text-center">Aucune réservation trouvée</td>
          </tr>
        <% } %>
      </tbody>
    </table>
  </div>
    <!-- Footer -->
<footer class="bg-light text-center py-4 mt-5">
  <p class="mb-1">&copy; <%= new Date().getFullYear() %> Hamzaoui Hamid - Tous droits réservés.</p>
  <ul class="list-inline">
      <li class="list-inline-item"><a href="/contact">Nous contacter</a></li>
      <li class="list-inline-item"><a href="#">Confidentialité</a></li>
      <li class="list-inline-item"><a href="#">Conditions</a></li>
  </ul>
</footer>
</body>
</html>

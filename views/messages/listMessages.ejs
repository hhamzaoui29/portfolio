<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Messages reçus</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/bootstrap.min.css">
  <link rel="stylesheet" href="/css/portfolio.css"> 

</head>
<body class="bg-light">
   <!-- Barre de navigation -->
   <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
      <!-- Bouton hamburger (mobile) -->
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>
      
      <div class="collapse navbar-collapse" id="navbarNav">
        <!-- Lien absolu vers ton portfolio -->
        <h1 class="h1">
          <a href="/projets" class="btn btn-secondary">⬅️ Retour à Liste Des Projets</a>
        </h1>
      </div>
    </div>
  </nav>
  
  <div class="container py-5">
    <h1 class="mb-4"> 📩 Messages reçus</h1>

     <!-- Bouton créer un Nouveau Message -->
     <div class="mt-4">
      <a href="/messages/formCreateMessage" class="btn btn-primary mb-3" >➕ Nouveau Message</a> 
    </div>

    <% if (messages.length === 0) { %>
      <p>Aucun message pour le moment.</p>
    <% } else { %>
      <table class="table table-striped">
        <thead>
          <tr>
            <th> Nom</th>
            <th> Email</th>
            <th>Titre du message</th>
            <th> Message</th>
            <th> Date</th>
            <th> Action</th>
          </tr>
        </thead>
        <tbody>
          <% messages.forEach(message => { %>
            <tr>
              <td><%= message.nom %></td>
              <td><%= message.email %></td>
              <td><%=message.titre%></td>
              <td><%= message.message %></td>
              <td><%= message.date %></td> 
              <td>
                <a href="/messages/showMessage/<%= message.id %>"       class="btn btn-sm btn-info">    👁️ Afficher</a>
              </td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    <% } %>
  </div>
  <!-- Footer -->
<footer class="bg-light text-center py-4 mt-5">
    <p class="mb-1">&copy; <%= new Date().getFullYear() %> Hamzaoui Hamid - Tous droits réservés.</p>
    <ul class="list-inline">
        <li class="list-inline-item"><a href="/contact">Nous contacter</a></li>
        <li class="list-inline-item"><a href="#">Confidentialité</a></li>
        <li class="list-inline-item"><a href="#">Conditions</a></li>
    </ul>
</footer>
</body>
</html>
